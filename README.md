# 《Web 开发技术》课程期末综合大作业任务书

**课程名称**：Web 开发技术
**适用对象**：计算机科学与技术专业 4231090301、4231090302、4231090303班
**项目形式**：个人独立完成
**项目名称**：基于 Electron 与 DeepSeek 的校园新闻智能采集助手

---

## 一、 项目背景与教学目标

本项目是《Web 开发技术》课程的期末核心考核环节。在掌握了 HTML、CSS、JavaScript 以及 Node.js 基础之后，学生能够综合运用上述技术，结合 **Electron** 桌面应用框架和 **SQLite** 嵌入式数据库，开发一个具有实际应用价值的桌面软件。

此外，紧跟当前 AIGC 技术趋势，本项目需要集成 **DeepSeek** 大语言模型接口，考察对第三方 API 的对接能力、异步编程处理能力以及"AI+应用"的落地思维。

**核心考察点：**
1.  **全栈思维**：理解客户端（Electron渲染进程）与服务端（Node.js主进程）的交互逻辑。
2.  **数据获取**：掌握网络爬虫技术（HTTP 请求与 DOM 解析）。
3.  **数据持久化**：掌握本地数据库（SQLite）的设计与增删改查。
4.  **智能化集成**：掌握 RESTful API 调用及 Prompt 工程基础。
5.  **工程化能力**：代码规范、异常处理、UI/UX 设计及技术文档撰写。

---

## 二、 系统功能需求详解

需要开发一个完整的 Electron 桌面应用程序（支持 Windows 或 macOS），系统需要包含以下功能模块：

### 1. 系统配置与 API 管理模块
由于大模型服务需要鉴权，且为了保护用户隐私，**严禁将 API Key 硬编码在源代码中**。
* **设置界面**：软件需要提供一个独立的"设置"页面或弹窗。
* **Key 管理**：可以输入个人的 DeepSeek API Key。
* **持久化存储**：输入的 Key 需要保存到本地数据库中，确保软件关闭重启后无需重新输入。
* **状态反馈**：界面应能直观显示当前 API Key 是否已配置（如显示"已配置/未配置"状态标签）。

### 2. 新闻数据采集模块 (Crawler)
需要内置爬虫引擎，针对 **河北地质大学官网 (hgu.edu.cn)** 进行定向数据采集。
* **目标网址**：以学校要闻板块 `https://www.hgu.edu.cn/info/1049/11082.htm` 为起始入口。
* **深度采集**：不仅要获取新闻列表页的信息，**需要解析列表中的链接，进入每一条新闻的详情页**抓取完整正文。
* **采集方式**：
    * 程序需要具备自动翻页或遍历列表的能力，持续采集新闻数据。
    * **"下一篇"导航**：河北地质大学学校要闻的新闻详情页中包含"下一篇"按钮，可以通过解析该按钮对应的 HTML 代码获取下一篇新闻的 URL，实现连续爬取。
    * **断点续传**：采集的新闻数据存入 SQLite 数据库后，下次启动程序时可以从指定的新闻开始继续爬取，或直接输入指定新闻的 URL 作为起始点，避免重复采集已有数据。
* **字段要求**：每条新闻需要包含以下字段：
    * `title` (标题)
    * `publish_date` (发布时间)
    * `source` (来源/作者)
    * `content` (正文纯文本，需要去除 HTML 标签干扰)
    * `original_url` (原文链接)
* **去重机制**：入库前需要进行查重（建议基于 URL 唯一性），防止重复爬取导致数据库冗余。
* **进度反馈**：爬取属于耗时操作，界面需要实时反馈进度（例如："正在抓取第 5 条..."），禁止界面在爬取时处于"假死"状态。

### 3. 数据管理与浏览模块 (UI/UX)
* **本地数据库**：使用 **SQLite** 存储所有抓取的数据。
* **列表展示**：
    * 设计美观的新闻列表页，展示新闻的关键元数据。
    * **分页功能**：需要实现完整的分页逻辑（每页展示数量合理，如 10 条/页），包含"首页"、"上一页"、"下一页"、"尾页"功能。
* **详情查看**：点击列表中的某一项，可以在新窗口或当前窗口的模态框（Modal）中查看新闻的完整内容。
* **批量操作**：新闻列表中每一项需要带有 **复选框 (Checkbox)**，支持全选或多选，用于后续的 AI 总结功能。

### 4. AI 智能摘要模块
结合 DeepSeek 大模型能力，实现对新闻内容的智能理解与压缩。
* **交互逻辑**：用户在列表中勾选一条或多条新闻后，点击“**智能总结**”按钮。
* **API 调用**：后端读取用户选中的新闻内容，构建合适的 Prompt（提示词），发送给 DeepSeek 接口。
* **结果展示**：
    * **单条模式**：如果仅选中一条，摘要结果应清晰地展示在该条新闻旁。
    * **综述模式**：如果选中多条，AI 应生成一份“新闻综述”，将多条新闻的核心内容整合，并在独立的弹窗或侧边栏中展示。
* **加载状态**：在等待 API 返回结果时，界面需要显示"正在生成摘要..."的 Loading 动画或骨架屏。

---

## 三、 推荐技术路线与开发规范

### 1. 技术栈建议
* **应用框架**：Electron (构建工具推荐 Electron Forge 或 Electron Builder)。
* **前端界面**：HTML5 + CSS3 + JavaScript (ES6+)。
    * *加分项*：使用 Vue.js、React 等现代前端框架。
    * *样式库*：允许使用 Bootstrap, Tailwind CSS, Element Plus, Ant Design 等。
* **后端逻辑**：Node.js 环境。
* **网络请求**：Axios / Node-fetch / Got。
* **HTML 解析**：
    * **Node.js 方案**：Cheerio / JSDOM / Puppeteer（推荐，与 Electron 集成更便捷）。
    * **Python 方案**：可以参考去年的项目，使用 Python + BeautifulSoup 进行爬虫开发。如果没有 Python 基础，建议直接使用 Node.js 编写。
* **数据库**：`sqlite3` / `better-sqlite3` / Sequelize / TypeORM。

### 2. 开发规范
* **工程结构**：项目目录结构清晰，逻辑代码（JS）、样式（CSS）、页面（HTML）与资源文件（Assets）应合理分离。MVC 分层清晰者佳。
* **安全通信**：严格遵守 Electron 安全规范，渲染进程与主进程的交互应通过 `preload.js` 中的 `contextBridge` 和 `ipcRenderer/ipcMain` 进行。**禁止**在渲染进程直接开启 `nodeIntegration: true`（除非在报告中详细说明了安全考量）。
* **代码质量**：变量命名规范（CamelCase），核心逻辑函数需要包含注释说明，代码需要具有良好的可读性。

---

## 四、 项目报告 (Report) 撰写要求

项目报告是评分的重要依据，请使用 **Markdown** 格式编写（文件名为 `REPORT.md`），字数不少于 1500 字。**注意**：项目报告需要独立提交，不包含在项目源码包中。同时需要使用 Typora 将 `REPORT.md` 导出为 PDF 格式（使用默认的 GitHub theme 主题），两个文件都需要提交。报告需要包含以下章节：

1.  **项目概述**：简述软件功能、解决的问题以及整体设计思路。
2.  **系统架构设计**：
    * 展示项目的目录结构树。
    * 绘制简单的数据流图或架构图（展示 Electron 主/渲染进程、爬虫、数据库、API 之间的关系）。
3.  **数据库设计**：
    * 提供 SQLite 的 E-R 图或详细的数据表字段定义（Schema）。
4.  **关键技术实现**（核心章节，需要结合代码片段讲解）：
    * **爬虫逻辑**：解释如何解析 DOM，如何处理异步并发或顺序爬取。
    * **Electron 通信**：展示 IPC 通信的代码片段并解释数据的流转过程。
    * **AI 接口对接**：说明 Prompt 的设计思路以及如何处理 API 的异步响应。
5.  **系统演示**：提供软件运行的关键截图（至少 4 张，覆盖设置页、爬取过程、列表页、摘要生成结果）。
6.  **遇到的问题与解决方案**：列举开发过程中遇到的至少 2 个技术难点及其解决过程（例如：如何解决异步回调地狱、如何处理网页结构变化等）。
7.  **安装与运行指南**：详细的步骤说明，确保助教可以顺利运行你的代码（包括 `npm install` 及启动命令）。

---

## 五、 评分标准 (满分 100 分)

| 评分维度 | 分值 | 详细考核点 |
| :--- | :---: | :--- |
| **1. 核心功能实现** | **60分** | **Electron 基础 (10分)**：应用启动正常，窗口管理无误，无严重报错。<br>**爬虫模块 (20分)**：成功抓取列表及详情页(10分)；实现自动翻页且数量达标(5分)；具备去重机制(5分)。<br>**数据库模块 (10分)**：SQLite 连接与 CRUD 功能正常，数据持久化有效。<br>**AI 摘要 (15分)**：Key 设置有效(5分)；API 调用成功(5分)；摘要生成逻辑正确(5分)。<br>**配置管理 (5分)**：配置项持久化存储。 |
| **2. 界面与交互体验** | **20分** | **UI 设计 (10分)**：界面布局整洁美观，CSS 样式完善，视觉体验良好。<br>**交互细节 (10分)**：分页逻辑正确(4分)；耗时操作有 Loading 提示(3分)；复选框与按钮逻辑顺畅(3分)。 |
| **3. 代码质量** | **10分** | **结构清晰 (5分)**：目录划分合理，前后端逻辑分离。<br>**规范性 (5分)**：命名规范，注释清晰，无大量冗余调试代码。 |
| **4. 项目报告** | **10分** | **完整性 (4分)**：章节齐全，Markdown 格式标准。<br>**深度 (4分)**：技术原理解释透彻，非简单代码堆砌。<br>**文档 (2分)**：运行指南准确，能够复现运行。 |

**加分项 (Extra Bonus, Max 10分)**：

* 使用 Vue / React 等现代前端框架构建界面。
* 实现了爬虫的定时自动任务（如每隔 24 小时自动更新）。
* 实现了对新闻内容的本地全文检索（Search）功能。
* 界面支持深色模式（Dark Mode）切换。

---

## 六、 提交方式

1.  **提交内容**（共 4 个文件）：
    * **项目源码包**：完整的项目源代码文件夹（**注意：务必删除 `node_modules` 文件夹**），包含数据库文件（可选，如果为了方便测试，可保留生成的 `.sqlite` 或 `.db` 文件）。压缩为 zip 格式，命名规范：`source.zip`（英文文件名）。
    * **可执行程序包**：一个独立的 zip 文档，包含编译好的可运行程序（使用 Electron Builder 或 Electron Forge 打包生成的应用程序）和数据库文件，确保助教可以直接运行测试，无需安装 Node.js 环境或执行 `npm install`。命名规范：`executable.zip`（英文文件名）。
    * **项目报告 Markdown**：项目报告 `report.md`，独立提交。
    * **项目报告 PDF**：将 `report.md` 使用 Typora 导出为 PDF 格式，导出时使用 Typora 默认的 GitHub theme 主题。命名规范：`report.pdf`。
2.  **打包格式**：所有文件均压缩为 `.zip` 格式（两个 zip 包）或直接提交（两个文档文件）。
3.  **截止日期**：请关注班级群通知。